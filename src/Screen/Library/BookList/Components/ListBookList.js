import React from 'react';
import {Text, View} from 'react-native';

const ListBookList = ({params}) => {
  return (
    <View>
      <Text>ListBookList</Text>
    </View>
  );
};

export default ListBookList;
